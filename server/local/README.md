# ローカルでwebserverを起動する簡単な方法

## npm で http-server をインストールする

```bash
$ npm install -g http-server
$ http-server --version
$ cd [起動したいディレクトリ]
$ http-server
```

## vscode で live server を使う

VSCode の右下にある「Go Live」をクリックする
